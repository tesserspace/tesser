# Tesser (Aggregate Facade)

Convenience crate that re-exports the key Tesser components under one dependency. Use this for downstream development when you want a single import with stable names.

```rust
use tesser::prelude::*;
```

See the root project README for full guides and examples.
